<.page title={gettext("Leaderboard")} size={:xl} title_class="uppercase">
  <div class="flex flex-col gap-12 mt-6">
    <div>
      <.day_selector
        day={@current_day_str}
        on_left="on_left"
        on_right="on_right"
        left_enabled={@left_enabled}
        right_enabled={@right_enabled}
      />
    </div>
    <div class="md:mt-0 mt-8">
      <.leaderboard
        :if={@leaderboard != []}
        entries={@leaderboard}
        user_position={@user_position}
      />
      <div :if={@leaderboard == []} class="flex flex-col items-center gap-4 my-20">
        <img src={~p"/images/dizzy-void.svg"} class="w-32 h-32" />
        <p>{gettext("Nenhuma informação disponível para mostrar sobre este dia.")}</p>
      </div>
    </div>
  </div>
  <div class="w-full flex flex-col items-center">
    <.prizes :if={@prizes != []} prizes={@prizes} />
  </div>
</.page>
